[OPEN] museum-map-load

# Debug Session: museum-map-load

## Symptom
- 地图模式加载不出来。

## Hypotheses
- H1: 复用了已有高德脚本节点，导致当前所需插件没有按新的参数加载。
- H2: `window.AMap` 已存在但能力不完整，`Map` 可用而 `plugin` 或 `ToolBar` 不可用。
- H3: 地图容器尺寸或挂载时机异常，地图实例没有完成初始化事件。
- H4: 初始化过程中存在运行时异常，但页面只显示为地图空白或加载失败。

## Plan
- 先添加运行时日志埋点，记录脚本来源、`AMap` 能力、容器尺寸、地图初始化关键步骤。
- 复现问题并分析日志，确认哪个假设成立。
- 基于证据做最小修复，再做修复后对比验证。
