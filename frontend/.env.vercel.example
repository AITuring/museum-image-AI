# Vercel Production environment for the cloud-only gallery.
# Set these same values in Vercel Project Settings -> Environment Variables.
VITE_CLOUD_ONLY=true
VITE_API_BASE_URL=https://api.aituring.xyz
