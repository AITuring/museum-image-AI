# Loaded by `npm run dev` (vite --mode gallery): 线上图库前端，固定端口 7001。
# 注：macOS 的 AirPlay 接收器占用 7000，故用 7001；如关闭 AirPlay 接收器可改回 7000。
#
# 这个前端连的是【线上/云端后端】，不是本地后端。做法与 Vercel 完全一致：
# VITE_API_BASE_URL 留空 => 浏览器同源请求 /api、/files，由 Vite dev proxy
# 转发到 VITE_CLOUD_BACKEND（见 vite.config.ts）。这样不需要云端配 CORS。
VITE_CLOUD_ONLY=true
VITE_API_BASE_URL=
# 云端后端地址（阿里云服务器）。换服务器时改这里即可。
VITE_CLOUD_BACKEND=http://123.57.34.90:8000
