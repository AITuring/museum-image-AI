# Quick entry only needs the cloud backend through the Vite same-origin proxy.
# Leave this blank in the repository and put the operator key in frontend/.env.quick.
VITE_QUICK_ENTRY_API_BASE_URL=
VITE_QUICK_ENTRY_TOKEN=
