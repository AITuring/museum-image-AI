# Optional server-side Vite proxy target. It is never exposed to browser code;
# leave it blank to reuse CLOUD_API_BASE_URL from the repository root.
QUICK_ENTRY_API_BASE_URL=
# Either put the operator key here or enter it in the page header.
VITE_QUICK_ENTRY_TOKEN=
